1.
 if we do not set this variable equal to blank first, it will equal to 'undefined' by default. Therefore there is a word 'undefined' before the result.
2.
 It is fine if we do not set this variable eqault to blank first since we have a if else statement below, which makes sure that this variable will be defined there. 